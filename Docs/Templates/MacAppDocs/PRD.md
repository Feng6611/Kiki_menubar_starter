# Product Requirements

## Purpose

`<App Name>` is a macOS `<menu bar/window/helper>` utility that helps users
`<solve user problem>`.

The app prioritizes `<stability/privacy/recovery/native behavior>` over
`<tempting but risky alternative>`.

## Target User

- `<primary user>`
- `<context where they need the app>`

## MVP Scope

- `<feature 1>`
- `<feature 2>`
- `<feature 3>`

## Explicitly Out Of Scope

- `<feature intentionally excluded>`
- `<permission intentionally not requested>`
- `<advanced architecture intentionally avoided>`

## Product Behavior

- `<core behavior rule>`
- `<settings behavior>`
- `<permission failure behavior>`
- `<access/paywall behavior, if any>`
- `<recovery behavior>`

## Privacy

- `<what data is read>`
- `<when it is read>`
- `<what is not stored>`
- `<what does not leave the device>`

## Success Criteria

- `<user can complete core flow>`
- `<failure path is understandable>`
- `<recovery path works>`
- `<tests or verification cover key rules>`

